# AGENTS.md

## Cursor Cloud specific instructions

This is a bare/empty frontend repository (`2nd-wave-dev-frontend`). As of the initial setup:

- The repo contains only a `README.md` with no application code, dependencies, or build configuration.
- There are no services to start, no tests to run, and no linting configured yet.
- When application code is added (e.g. a React/Next.js/Vue app), this file should be updated with:
  - How to install dependencies (e.g. `npm install`, `pnpm install`)
  - How to start the dev server and its port
  - How to run tests and linting
  - Any environment variables or secrets required
